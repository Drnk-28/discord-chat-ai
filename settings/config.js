module.exports = {
   TOKEN: process.env.TOKEN || "", // your discord bot token.
   cohere: process.env.cohere || "", // cohere api key.
   clientId: process.env.clientId || "", // your discord bot id.
   prefix: process.env.prefix || "" // your discord bot prefix.
}